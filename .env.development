# Development defaults (loaded by `vite` dev server, incl. Playwright e2e).
# Not loaded for production builds, so the published Docker image keeps the
# Simple/Full toggle hidden by default unless VITE_QR_CREATE_SIMPLE_FULL_MODE_TOGGLE
# is explicitly passed at build time.
VITE_QR_CREATE_SIMPLE_FULL_MODE_TOGGLE=true
